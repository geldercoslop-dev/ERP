# VariÃ¡veis de ambiente para produÃ§Ã£o.
# Copie para a raiz do projeto como .env.production ou configure no sistema/servidor.
# cp deployment/env.example .env.production

NODE_ENV=production
PORT=3000

# Banco (obrigatÃ³rio)
DATABASE_URL=mysql://usuario:senha@host:3306/nome_do_banco

# AutenticaÃ§Ã£o (obrigatÃ³rio em produÃ§Ã£o)
JWT_SECRET=defina_um_secret_longo_e_aleatorio_aqui

# Opcional: senha inicial do admin (se usar seed)
# ADMIN_INITIAL_PASSWORD=...

# Opcional: URL do tRPC para o cliente (mesma origem = /api/trpc)
# VITE_TRPC_URL=/api/trpc

# Opcional: CORS (origens permitidas separadas por vÃ­rgula)
# ALLOWED_ORIGINS=https://erp.seudominio.com

# Observabilidade (recomendado em produÃ§Ã£o)
# SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx
# SENTRY_TRACES_SAMPLE_RATE=0.2
# SENTRY_SEND_PII=0
# Rota GET /api/test-error (erro intencional) â€” em produÃ§Ã£o exige ENABLE_SENTRY_TEST_ROUTE=1
# ENABLE_SENTRY_TEST_ROUTE=0
# LOG_LEVEL=info
