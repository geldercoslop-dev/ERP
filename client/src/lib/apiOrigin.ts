/**
 * Origem HTTP padrão da API em desenvolvimento local.
 * O proxy do Vite encaminha `/api` → esta origem (ver vite.config na raiz e em client/).
 */
export const GRS_API_ORIGIN = "http://localhost:3000";
